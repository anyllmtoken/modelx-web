# Site URL (required for production deployment)
# Used in sitemap, robots.txt, JSON-LD structured data, OG images, RSS feed, etc.
SITE_URL=https://modelpedia.dev

# API URL (optional, defaults to SITE_URL + "/api" if not set)
# Used for API endpoint documentation and export links
API_URL=https://api.anyllmtoken.com
